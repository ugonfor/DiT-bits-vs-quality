name: dit-bits-vs-quality
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - cudatoolkit=11.8
  - pip>=23.0
  - pip:
      - torch==2.5.0 --index-url https://download.pytorch.org/whl/cu124
      - torchvision==0.20.0 --index-url https://download.pytorch.org/whl/cu124
      - torchaudio==2.5.0 --index-url https://download.pytorch.org/whl/cu124

      - diffusers==0.34.0
      - transformers==4.41.0
      - accelerate==0.29.3
      - safetensors==0.4.4
      - pillow>=10.0
      - tqdm>=4.66
      - huggingface_hub>=0.23
      - sentencepiece
      - protobuf
      - tensorboard
      - bitsandbytes
      - image-reward
      - clip
      - pre-commit
      - matplotlib
